# Generated analysis outputs
outputs/

# The data arrays (data/*.npy) are versioned with Git LFS, not ignored.
# A normal clone fetches them automatically when Git LFS is installed.

__pycache__/
*.pyc
